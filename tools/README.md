# Tools

Utility scripts for working with the Orinbot workspace. Make scripts executable with `chmod +x` when adding new ones.

- `export_pcd.sh`: Record a short bag and convert it to a PCD directory for inspection.
- `replay_bag.sh`: Play back recorded rosbags with configurable rate and loop settings.
- `fix_timestamps.py`: Normalize or repair message timestamps before offline processing.
