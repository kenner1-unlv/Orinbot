# Point Cloud Exports

Exported PCD files from bags or live captures are stored here. Use `../tools/export_pcd.sh` to generate new captures.

The directory is ignored by git to prevent large binary files from being committed.
